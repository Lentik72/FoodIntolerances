//
//  ChartsComponents.swift
//  Food IntolerancesI am choosing options
//
//  Created by Leo on [Date].
//

//import SwiftUI
//import Charts

//struct SeverityDistributionChart: View {
   // let logs: [LogEntry]
    
   // var body: some View {
       // let distribution = Dictionary(grouping: logs, by: \.severity)
           // .map { (severity, logs) in (severity, logs.count) }
           // .sorted { $0.0 < $1.0 }
        
       // Chart(distribution, id: \.0) { data in
           // BarMark(
              //  x: .value("Severity", String(data.0)),
              //  y: .value("Count", data.1)
          //  )
           // .foregroundStyle(colorForSeverity(data.0))
      //  }
   // }
    
   // private func colorForSeverity(_ severity: Int) -> Color {
      //  switch severity {
      //  case 1: return .green
      //  case 2: return .yellow
      //  case 3: return .orange
     //   case 4: return .red
     //   case 5: return .purple
     //   default: return .gray
      //  }
  //  }
//}
